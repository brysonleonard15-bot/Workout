# Workout
Workout Builder &amp; Log is a lightweight, browser-based workout planning and tracking tool that lets you build structured training sessions, auto-populate intensity templates, and save your workouts locally — all inside a single HTML file with no backend or login required
